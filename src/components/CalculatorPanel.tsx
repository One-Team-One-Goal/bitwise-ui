// Minimal placeholder CalculatorPanel to ensure a default export is provided
// Replace or extend this with the full implementation when ready
import React from 'react'

const CalculatorPanel: React.FC = () => {
  return (
    <div className="calculator-panel-placeholder" style={{padding: 12}}>
      {/* Lightweight placeholder to prevent import errors while developing the full panel */}
      <strong>Calculator Panel</strong>
    </div>
  )
}

export default CalculatorPanel
